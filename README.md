# react-server-explorer

Install dependancies
```
npm i
```

To run the app in development mode:

```
npm run dev
```


To run the app in production mode:
```
npm run prod
http-server // to load the app with local server
```

To run testing coverage:
```
npm run test
```
